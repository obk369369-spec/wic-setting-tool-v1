# TOOL042 customer-work entry

New customer work starts through `scripts/customer_work_start.js`, with customer_id and explicit materials_required. Public branch/copy exports and CLI route through this entry. It loads pinned CENTRAL and tool masters/checkpoints, actual feedback, private CLEAN customer master and shared release-gate source. `generateFromContext` and guidance `--emit-if-pass` must pass the same final gate. `_customer_branch_internal.js` is private generation/fixture code, never a release API. Caller-supplied verified or semantic PASS flags cannot authorize output.

Missing current customer/history, ambiguous identity, source authentication failure or missing actual materials is HOLD with no customer draft. The shared Python gate is invoked with WIC_BUNDLED_PYTHON (or python); missing runtime is HOLD. Its semantic evaluator is NOT connected: all customer release is withheld, including rewritten historical questions. Native CENTRAL save uses a redacted CAS update/read-back of the existing root; live authentication remains unverified. This repository instruction is not a global ChatGPT/Work interception hook. Reuse existing PASS evidence; no full audit or unchanged test replay.
